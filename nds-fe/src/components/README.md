# Components

재사용 가능한 컴포넌트들을 여기에 작성합니다.

## 구조
```
components/
  ├── common/          # 공통 컴포넌트 (Button, Input, Modal 등)
  ├── layout/          # 레이아웃 컴포넌트 (Header, Footer, Sidebar 등)
  └── features/        # 특정 기능 컴포넌트
```

